.DS_Store
node_modules
.turbo
*.log
.next
.vercel
.react-router
.trigger
.sst
dist
dist-ssr
*.local
.cache
server/dist
public/dist
vite.config.ts.timestamp*
.claude-flow
.claude/settings.local.json
.claude/scheduled_tasks.lock
.mcp.json
opencode.json
.windsurf

# Lingui compiled catalogs
packages/locale/locales/**/*.mjs

# Generated PDF font data (woff base64) — built on postinstall + turbo build
packages/documents/src/pdf/fonts.data.ts

scripts/migrations/data
scripts/paperless-test.ts
scripts/test-get-stripe-customer.ts

# local env files
.env
.envrc
.env.local
.env.dev
.env.development.local
.env.test.local
.env.prod
.env.production
.env.production.local
!packages/apps/**/.env
packages/database/.env
packages/database/supabase/seed.sql

.env*.local

# Rust
**/target/
**/*.rs.bk
**/pkg/

# C++ (cad-engine)
**/build/
!**/build/.gitkeep
.orcka
**/supabase/snippets

# Worktree-isolated dev environment
.carbon-worktree
.superpowers

# Agent scratch — local-only, never commit
.claude/scratch/
apps/*/portless.json
packages/database/supabase/snippets/**
.pr-review

# .claude/ and .codex/ rules+skills are symlinks created by .ai/scripts/install-skills.sh
# Source of truth is .ai/rules/ and .ai/skills/ — never edit under .claude/ or .codex/ directly
.claude/rules/
.claude/skills/
.claude/notes/
.codex/

# .ai/ scratch — runtime, not source
.ai/scratch/

# Loop run artifacts (@carbon/harness RUNS_DIR) — directories only, so
# committed markdown run logs (.ai/runs/*.md) stay tracked
.ai/runs/*/

# Legacy llm/ — consolidated into .ai/ (docs, specs, runs)
llm/
